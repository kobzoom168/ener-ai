services:
  ener-ai:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./backups:/app/backups
      - /root/.codex:/root/.codex
      - /root/ener-ai:/root/ener-ai
    env_file:
      - .env
    restart: unless-stopped
